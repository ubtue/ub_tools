##
# This is a template for new installation configurations.
# Please copy this file befor changing!
#
# You can delete unused settings.
##

# The following settings are required!
NAME="MyCopy"
VUFIND_CLONE_DIRECTORY=$(pwd)/$NAME
VUFIND_REPOSITORY="git@github.com:ubtue/$NAME.git"
UB_TOOLS_CLONE_DIRECTORY=$(pwd)/ub_tools
UB_TOOLS_REPOSITORY="git@github.com:ubtue/ub_tools.git"
SERVER_URL="localhost"                   # Without http://
SERVER_IP="127.0.0.1"
EMAIL="support@localhost"
MODULES="MyModule1,MyModule2,MyModule3"
CONFIGS_DIRETORY=$UB_TOOLS_CLONE_DIRECTORY/configs/$NAME
USER_NAME=vufind
USER_GROUP=vufind

# The following settings are optional and can be deleted.
SSL_CERT=""                              # if set, provides https
SSL_KEY=""                               # if set, provides https
SSL_CHAIN_FILE=""
FORCE_SSL=false                          # if true, redirects http to https
HTPASSWD=""                              # if set, provides password protection
CRONJOBS=''                              # A list of cronjobs (one per line)